import './App.css';
import { Fragment } from 'react';

function AppJSX() {
    return (
        <Fragment>
            <div></div>
        </Fragment>
    );
}
// 컴포넌트는 하나의 태그를 반환해야 한다.
// 함수의 이름은 대문자로 시작해야 한다.
export default AppJSX;
